<ul>
  <% if Admin::NewsesController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_newses_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_newse_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::NewsesController.sortable? and Newse.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Newses"),
                 admin_newses_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Newses"),
                 admin_newses_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>