<%= form_for(@feedback) do |f| %>
  <% if @feedback.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feedback.errors.count, "error") %> prohibited this feedback from being saved:</h2>

      <ul>
      <% @feedback.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name,:class=>'label' %><br />
    <%= f.text_field :name,:class=>'box' %>
  </div>
  <div class="field">
    <%= f.label :email, "Email Address",:class=>'label' %><br />
    <%= f.text_field :email,:class=>'box' %>
  </div>
  <div class="field">
    <%= f.label :description,'Comment',:class=>'label' %><br />
    <%= f.text_area :description,:class=>'feld' %>
  </div>
  <div class="actions">
  <input class ="submit" id="salesforce_lead_submit" name="commit" type="submit" value="SEND" align="center" onclick='return validate_feedback()'/>
  </div>
<% end %>
